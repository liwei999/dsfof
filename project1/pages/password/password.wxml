<view class="container1">
	<view class="container1-body">
		<view class="group">
			<view class="group-body">
				<view class="img-upload">
					<view class="img-add" bindtap="chooseImage"></view>
					<view class="img-item" bindlongtap="editImage" wx:for="{{uploadimgs}}" wx:key="{{index}}">
						<image src="{{item}}" mode="aspectFill"></image>
					</view>
				</view>
			</view>
			<view class="group-body">
				<view class="input-list">
					<view class="input-item">
						<text class="input-item-label">新密码</text>
						<view class="input-item-content">
							<input type="text" password='false' placeholder="请输入您的新密码" bindinput="inputName"></input>
						</view>
					</view>
					<view class="input-item">
						<text class="input-item-label">确认密码</text>
						<view class="input-item-content">
							<input type="text" password='false' placeholder="请再次确认密码" bindinput="inputPhone"></input>
						</view>
					</view>
				</view>
			</view>
		</view>
		<view class="btn-submit">
			<button class="btn-orange" bindtap="passwordSubmit">确认</button>
		</view>
		<view class="password-text">
			<text>如密码无法简修改成功</text>
			<text>可以直接拨打电话</text>
			<text>我们将第一时间为您答疑解惑</text>
			<view bindtap="callContact">028-86263615</view>
		</view>
	</view>
</view>